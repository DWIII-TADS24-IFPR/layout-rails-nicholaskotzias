<nav class="bg-white shadow-md">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      
      <div class="flex space-x-4 items-center">
        <%= link_to "Home", root_path, class: "text-gray-800 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium" %>
        <%= link_to "Posts", posts_path, class: "text-gray-800 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium" %>
        <%= link_to "Comments", "#", class: "text-gray-800 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium" %>
      </div>

      <div class="flex items-center space-x-4">
        <% if user_signed_in? %>
          <span class="text-gray-800 text-sm font-medium"><%= current_user.email %></span>
            <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "text-white bg-blue-600 hover:bg-blue-700 px-3 py-2 rounded-md text-sm font-medium" %>
        <% else %>
          <%= link_to "Sign In", new_user_session_path, class: "text-gray-800 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "text-blue-600 border border-blue-600 hover:bg-blue-50 px-3 py-2 rounded-md text-sm font-medium" %>
        <% end %>
      </div>

    </div>
  </div>
</nav>
