<div class="w-full bg-white shadow-sm rounded-md p-5 flex flex-col sm:flex-row justify-between items-start sm:items-center">
  <div class="flex-1">
    <h2 class="text-2xl font-semibold text-gray-800"><%= post.title %></h2>
    <p class="text-gray-600 mt-2"><%= truncate(post.body, length: 150) %></p>
  </div>
  <div class="mt-4 sm:mt-0 flex space-x-2">
    <span class="text-gray-500 text-sm">Created at: <%= post.created_at.strftime("%b %d, %Y") %></span>
  </div>
</div>